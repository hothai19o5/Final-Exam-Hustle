import ExamTickerPage from '@/components/exam-ticker-page';

export default function Home() {
  return (
    <main className="flex flex-col items-center justify-center bg-background text-foreground">
      <ExamTickerPage />
    </main>
  );
}
